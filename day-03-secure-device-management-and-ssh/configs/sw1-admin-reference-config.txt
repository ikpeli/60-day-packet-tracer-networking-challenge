! DAY 3 SANITIZED REFERENCE — SW1-ADMIN
enable
configure terminal
hostname SW1-ADMIN
no ip domain-lookup
enable secret <LAB_ENABLE_SECRET>
service password-encryption
username netadmin privilege 15 secret <LAB_SSH_SECRET>
ip domain-name portfolio.local
banner motd ^CAUTHORISED USERS ONLY^C

vlan 10
 name ADMIN-MGMT
 exit

interface FastEthernet0/1
 description TO_ADMIN_PC
 switchport mode access
 switchport access vlan 10
 exit

interface GigabitEthernet0/1
 description TO_R1_G0_0
 switchport mode access
 switchport access vlan 10
 exit

interface Vlan10
 description MANAGEMENT_SVI
 ip address 192.168.10.2 255.255.255.0
 no shutdown
 exit

ip default-gateway 192.168.10.1

line console 0
 password <LAB_CONSOLE_PASSWORD>
 login
 logging synchronous
 exec-timeout 5 0
 exit

line vty 0 4
 login local
 transport input ssh
 exec-timeout 5 0
 exit

line vty 5 15
 login local
 transport input ssh
 exec-timeout 5 0
 exit

ip ssh version 2
end

! Run from privileged EXEC mode after hostname and domain configuration:
! crypto key generate rsa modulus 1024
! copy running-config startup-config
